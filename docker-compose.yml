services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    environment:
      - OLLAMA_BASE_URL=${OLLAMA_BASE_URL:-http://host.docker.internal:11434}
      - MODEL_TIMEOUT_SECONDS=${MODEL_TIMEOUT_SECONDS:-300}
      - ALLOWED_ORIGINS=${ALLOWED_ORIGINS:-http://localhost:8080,http://127.0.0.1:8080}
      - ADMIN_PANEL_TOKEN=${ADMIN_PANEL_TOKEN:?ADMIN_PANEL_TOKEN måste sättas, se README}
      - PROVIDER_ENCRYPTION_KEY=${PROVIDER_ENCRYPTION_KEY:?PROVIDER_ENCRYPTION_KEY måste sättas, se README}
    ports:
      - "127.0.0.1:8001:8001"
    extra_hosts:
      - "host.docker.internal:host-gateway"

  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      - backend
    ports:
      - "8080:80"
